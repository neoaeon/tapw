<%= form_for(@poi_cat) do |f| %>
  <% if @poi_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poi_cat.errors.count, "error") %> prohibited this poi_cat from being saved:</h2>

      <ul>
      <% @poi_cat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uid %><br />
    <%= f.text_field :uid %>
  </div>
  <div class="field">
    <%= f.label :mapuid %><br />
    <%= f.text_field :mapuid %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
